# ========================================
# MAGILOC - Configuration Backend
# ========================================

# Base de données PostgreSQL
# Render: Copie l'External Database URL
DATABASE_URL=postgresql://user:password@host:port/database

# Port du serveur
PORT=5000

# Environnement
NODE_ENV=development

# Configuration DB locale (optionnel)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=magiloc
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_SSL=false
