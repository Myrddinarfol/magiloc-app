{
  "name": "magiloc",
  "version": "1.0.0",
  "description": "MagiLoc - Equipment Management System",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm ci --legacy-peer-deps && npm run build"
  }
}
